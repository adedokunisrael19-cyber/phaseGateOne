from unittest import TestCase

import Number_array

class TestArrayNumber(TestCase):

    def test_returns_the_factors_from_an_array(self):
        is_valid = Number_array.ArrayNumber(number, arr)
        self.assertEqual(is_valid)
   
